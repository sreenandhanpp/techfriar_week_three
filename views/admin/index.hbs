<section>
    <nav class="navbar navbar-expand-lg py-4">
        <div class="container">
            <a class="navbar-brand" href="#">
                <img src="images/logo.png" alt="">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
                aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse fs-5" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">

                    <li class="nav-item">
                        <a class="nav-link" href="#">All users</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">All products</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Graph</a>
                    </li>
                </ul>
                {{!-- <div class="input-group w-25 d-flex justify-content-end">
                    <form action="/logout" class="" method="get">
                        <button class="btn btn-outline-danger " type="submit" id="button-addon2">Logout</button>
                    </form>
                </div> --}}
                <div class="dropdown">
                    <a class="btn btn-info dropdown-toggle text-white"  href="#" role="button" data-bs-toggle="dropdown"
                        aria-expanded="false">
                        {{userName}}
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item text-danger" href="/logout">Logout</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </nav>
    
    <section>
        <div class="container">
            <div class="row">
                <div class="col-md-12 d-flex justify-content-end mt-5">
                    <a href="/admin/create-user" class="btn btn-success"> <i class="fa-solid fa-plus"></i></a>
                </div>
            </div>
        </div>
        <div class="container p-5">
            <div class="row d-flex justify-content-center">
                <div class="col-md-12">
                    <table class="table" id="myTable">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Name</th>
                                <th scope="col">Email</th>
                                <th scope="col">Handle</th>
                            </tr>
                        </thead>
                        <tbody>
                            {{#each data}}
                            <tr>
                                <th scope="row">1</th>
                                <td>{{ this.name }}</td>
                                <td>{{ this.email }}</td>
                                <td>
                                    <a href="/admin/edit-user/{{this._id}}" class="btn btn-primary">Edit</a>
                                    <a href="/admin/delete-user/{{this._id}}" class="btn btn-danger"
                                        onclick="return confirm('Are you sure to delete {{this.name}} user')">Delete</a>
                                </td>
                            </tr>
                            {{/each}}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

    </section>
</section>

<script>
    $(document).ready(function () {
        $('#myTable').DataTable();
    });
</script>